import 'package:flutter/material.dart';

class ShoppingCart extends StatelessWidget {

  late final name;
  late final price;

  ShoppingCart({required this.name, required this.price});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
